# asynchronous-programming, file-access
Options for writing/reading using `FileStream` and `File`  
filestream writes bites in async mode
In $"file-{index:00}.txt";  {index:00}  is  approach to format   
